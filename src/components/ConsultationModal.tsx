import { X } from 'lucide-react';
import { useEffect } from 'react';
export function ConsultationModal({studio,onClose}:{studio:string;onClose:()=>void}){useEffect(()=>{const close=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[onClose]);return <div className="modal-backdrop" onMouseDown={onClose}><form className="consult-modal" onMouseDown={e=>e.stopPropagation()} onSubmit={e=>{e.preventDefault();onClose()}}><button className="close" type="button" onClick={onClose}><X/></button><p className="mono">{studio}</p><h2>Request a consultation</h2><p className="muted">A short introduction is the best place to begin.</p><input required placeholder="Your name"/><input required type="email" placeholder="Email address"/><select defaultValue=""><option value="" disabled>What are you planning?</option><option>New home</option><option>Renovation</option><option>Interior design</option><option>Commercial project</option></select><textarea placeholder="Tell us a little about the project"/><button className="dark-button">Send request ↗</button></form></div>}
